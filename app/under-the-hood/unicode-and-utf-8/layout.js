export const metadata = {
    title: 'Unicode and UTF-8',
    description: 'Visualize how text becomes bytes — code points, UTF-8 variable-length encoding and its prefix bits, UTF-16 surrogate pairs, grapheme clusters, ZWJ emoji, and normalization.',
    keywords: ['unicode', 'utf-8', 'utf-16', 'code point', 'surrogate pair', 'grapheme cluster', 'zwj emoji', 'normalization', 'nfc nfd', 'mojibake', 'character encoding'],
};
export default function Layout({ children }) { return children; }
