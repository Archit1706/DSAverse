export const metadata = {
    title: "Queue (Array) Visualizer – Enqueue & Dequeue Animation",
    description: "Visualize a Queue implemented with an array: enqueue at the rear, dequeue from the front. FIFO (First In First Out) data structure. See circular buffer behavior with O(1) operations.",
    keywords: ["queue array", "queue data structure", "enqueue dequeue", "FIFO", "circular buffer", "data structure visualization"],
    openGraph: {
        title: "Queue (Array Implementation) Visualizer – DSAverse",
        description: "Interactive Queue array visualization with enqueue and dequeue animations.",
        images: [{ url: "/og-image.png" }],
    },
};
export default function Layout({ children }) { return children; }
